export const scheduledSyncOrders = (req, res) => {
  res.json({ message: 'scheduledSyncOrders OK' });
};

export const processNFeQueue = (req, res) => {
  res.json({ message: 'processNFeQueue OK' });
};